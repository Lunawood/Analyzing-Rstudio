# 숫자를 배열로 저장할 때
data <- c(1,2,3,4,5)
data[2]

# help(모르는 함수) : 잘 모르는 함수에 대해 검색 가능
help("data")

# data() 데이터셋을 가져오기 위해서 data() 함수를 호출
data(mtcars)

# head() 앞 부분의 일부 데이터(기본 6개)만 불러 올 수 있다.
head(mtcars, 1)

# summary() 데이터에 대한 요약 정보를 나타냄
summary(mtcars)
